---
name: Default Launch Resolution
status: open
created: 2026-05-20T07:56:41Z
updated: 2026-05-20T08:03:13Z
github: https://github.com/wubq511/cc-profile-switch/issues/19
depends_on: [17]
parallel: true
conflicts_with: [18]
---

# Task: Default Launch Resolution

## Description

Allow `ccps launch` to omit the profile argument when `config.json.defaultProfile` is set, while preserving all existing launch and dry-run behavior.

## Acceptance Criteria

- [ ] `ccps launch [profile]` accepts an optional profile argument.
- [ ] `ccps launch --dry-run` resolves to `defaultProfile` when no profile argument is provided.
- [ ] `ccps launch` resolves to `defaultProfile` when no profile argument is provided.
- [ ] Launch with an explicit profile still takes precedence over default.
- [ ] Launch without an explicit profile and without a default fails clearly with next-step guidance.
- [ ] Launch fails clearly if `defaultProfile` points to a missing profile.
- [ ] Existing dry-run parity, cwd handling, MCP merge behavior, API env behavior, plugin directory behavior, skip-permissions default, and memory isolation remain unchanged.

## Technical Details

- Prefer a shared `resolveLaunchProfile` core helper used by both `buildLaunchPlan` and `launchProfile`.
- Keep `formatLaunchDryRun` output explicit about which profile was selected.
- Update existing launcher tests rather than replacing them.

## Dependencies

- 17

## Effort Estimate

- Size: M
- Hours: 3

## Definition of Done

- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
