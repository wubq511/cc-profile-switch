---
name: Backup and Edit Commands
status: closed
created: 2026-05-16T09:37:37Z
updated: 2026-05-16T14:41:49Z
github: https://github.com/wubq511/cc-profile-switch/issues/7
depends_on: [4]
parallel: true
conflicts_with: [5, 6]
---

# Task: Backup and Edit Commands

## Description

Implement `ccps backup <name>` and `ccps edit <name> [file]` with strict path boundaries and clear file mappings.

## Acceptance Criteria

- [x] `ccps backup <name>` copies the selected profile into `backups\<name>-YYYYMMDD-HHmmss`.
- [x] Backup includes `profile.json`, `claude-home`, `mcp.json`, and `plugins`.
- [x] Backup does not modify the source profile.
- [x] `ccps edit <name>` opens the selected profile directory.
- [x] `ccps edit <name> CLAUDE.md` opens `claude-home\CLAUDE.md`.
- [x] `ccps edit <name> settings.json` opens `claude-home\settings.json`.
- [x] `ccps edit <name> mcp.json` opens profile-level `mcp.json`.
- [x] `ccps edit <name> profile.json` opens profile-level `profile.json`.
- [x] Any other edit target is rejected without opening a path.
- [x] Tests cover backup timestamp format, backup contents, approved edit mappings, and rejected path traversal.

## Technical Details

- Implement editor/default opener behavior in `src/platform/editor.ts`.
- Use stable timestamp formatting for backup directory names.
- Do not prompt to create arbitrary files outside the approved mapping.
- Never open or edit `C:\Users\h\.claude`.
- Keep process launch for editor separate from Claude Code launch logic.

## Dependencies

- Depends on `4` for profile path resolution and template structure.

## Effort Estimate

- Size: M
- Hours: 5

## Definition of Done

- [x] Code implemented
- [x] Tests written and passing
- [x] Code reviewed
