---
name: Real Launch and README
status: closed
created: 2026-05-16T09:37:37Z
updated: 2026-05-17T05:02:46Z
github: https://github.com/wubq511/cc-profile-switch/issues/10
depends_on: [8, 9]
parallel: false
conflicts_with: [8]
---

# Task: Real Launch and README

## Description

Implement real `ccps launch <name>` process spawning after validation, dry-run, and manual verification are in place. Finish the MVP README so a Windows user can run the full workflow safely.

## Acceptance Criteria

- [x] Real launch validates the selected profile before spawning Claude Code.
- [x] Real launch uses `spawn('claude', args, { cwd, stdio: 'inherit', shell: false, env })`.
- [x] Real launch sets `CLAUDE_CONFIG_DIR` to the selected profile's `claude-home`.
- [x] Real launch keeps cwd as the current project directory unless a safe explicit cwd option is provided.
- [x] Real launch never uses `--add-dir` for the current project.
- [x] Real launch never changes cwd to `.cc-profile-switch` or any runtime directory.
- [x] Launch failure messages are clear and include next-step guidance.
- [x] Last-used metadata is updated only after a valid launch attempt reaches the intended execution point.
- [x] README documents install, local development, commands, profile structure, safety boundaries, dry-run, launch behavior, and verification steps.
- [x] `npm run check` passes.

## Technical Details

- Keep real process spawning in `src/platform/process.ts` and core launch decisions in `src/core/launcher.ts`.
- Do not build shell command strings for execution.
- Preserve the default MCP merge behavior.
- If verification showed Claude Code limitations, document them and reflect them in launch warnings.
- README must clearly state that the tool does not migrate OAuth/session/token/history/cache.

## Dependencies

- Depends on `8` for launch-plan logic.
- Depends on `9` so real launch is implemented with documented Claude Code behavior assumptions.

## Effort Estimate

- Size: L
- Hours: 8

## Definition of Done

- [x] Code implemented
- [x] Tests written and passing
- [x] Code reviewed
