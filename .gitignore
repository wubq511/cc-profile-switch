node_modules/
dist/
coverage/
probe-output/
# Materialized fixture trees are generated on demand by the deterministic
# generator (npm run fixtures:generate); never commit generated fixtures.
.fixtures-out/
.env
.env.*
*.log
.claude/skills/
skills-lock.json
.DS_Store
# Machine-specific perf results (generated by npm run perf:harness -- --out)
docs/acceptance/*-perf.json
# Local tool state (ccpm/Claude worktree preference) — machine-local, not
# project content; .claude/epics stays tracked, .claude/skills stays ignored.
.claude/settings.json
# Issue #97 visual-audit tooling byproducts (the full frame archive stays
# uncommitted; cited evidence frames are committed under
# docs/acceptance/workbench-visual-audit-97/shots/)
.audit-venv/
audit-output/
